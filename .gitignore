# Hugo build output — never commit, regenerated on every build
public/
resources/_gen/
.hugo_build.lock

# macOS system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Node (if ever used)
node_modules/
npm-debug.log*

# Environment & secrets — never commit
.env
.env.local
.env.*.local

# Logs
*.log
